# Faraway Session 9 — 2026-05-21

## Post-session-8 Audit Fixes (12 items)

### UI Layout Fixes

1. **Minimap moved to top-right** (`MY = 40`) — clears all canvas HUD button conflicts.
   Previously at `MY = canvas.height - MH - 70` which overlapped the [?][♪][✦] HUD buttons and the mobile action panel.

2. **Survival bars moved to top-left** (`by2 = 110`) — below resource HUD + era label + training queue row.
   Previously at `canvas.height - 80` which placed them under the d-pad on mobile.

3. **Mobile utility buttons added** (`#util-btns` HTML div, centre strip between d-pad and action panel):
   - H — toggle help panel
   - ? — toggle info guide
   - ♪ — mute/unmute music
   - ✦ — save & exit
   Previously the canvas HUD buttons (bottom-right) were buried under the mobile action panel (also bottom-right).

### Code Fixes

4. **Save-exit loop bug fixed** — added early return at top of `loop()` when `saveExitPending` is true.
   Previously the RAF restart in the catch block re-ran full game logic with `saveExitPending=true`, creating a broken state.

5. **Help panel updated** — added `[♪ icon]` and `[✦ icon]` entries in the ACTIONS section.
   Footer updated to: `[ H ] [ ? ] Esc close · ♪ mute · ✦ save & exit`.

6. **Info panel nav hint updated** — "Arrow keys / Swipe to navigate · Esc to close".

7. **Building prompt font** — 9px → 10px; width calculation updated to `hint.length * 7 + 18`.

8. **Carbon Credits in resource HUD** — added 5th box (teal "CC:N") alongside W/S/F/C.
   `carbonCredits` field existed in SaveState but was never displayed.

9. **Training queue indicator** — shows "⚒ N training (Xs)" below era label when `trainQueue.length > 0`.

10. **`triggerSaveAndExit` exported** from `game.ts` for direct mobile DOM wiring.

11. **`toggleMusicUI` exported** from `game.ts` (wraps `initAudio()` + `toggleMuteMusic()`).

12. **`main.ts`** — imports `triggerSaveAndExit` and `toggleMusicUI`; wires `btn-util-mute` and `btn-util-exit` to direct calls; wires `btn-util-help`/`btn-util-info` via `tapKey`.

### Files Changed
- `src/game.ts` — loop fix, minimap, survival bars, resource HUD (CC + training queue), building prompts, help panel, info panel, exports
- `src/main.ts` — import new exports, wire utility buttons
- `index.html` — `#util-btns` HTML + CSS (`.util-btn`, `#btn-util-*` styles)

### TypeScript
`npx tsc --noEmit` — clean, no errors.
